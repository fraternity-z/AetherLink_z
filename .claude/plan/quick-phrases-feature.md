# 快捷短语功能 - 实施规划文档

> 生成时间：2025-11-13
> 状态：待讨论与确认
> 预估工作量：12-15 小时

---

## 项目目标

在 AetherLink_z 聊天应用中实现快捷短语功能，允许用户：

1. 在设置页面管理常用短语（增删改查）
2. 在主对话界面通过双击空白区域快速选择和插入短语到输入框
3. 提供流畅的用户体验和直观的交互设计

---

## 已明确的技术决策

- **技术栈**: React Native (Expo) + TypeScript
- **数据存储**: Expo SQLite（遵循现有 Repository 模式）
- **UI 框架**: React Native Paper + React Native Elements
- **弹窗系统**: 使用现有的 ConfirmDialog 和 InputDialog 组件
- **设置页面**: 已有快捷短语占位页面 (`app/settings/quick-phrases.tsx`)
- **手势检测**: 使用 React Native Gesture Handler 检测双击
- **路由系统**: Expo Router（文件路由）

---

## 详细任务分解

### 阶段 1：数据层设计与实现

#### 任务 1.1：设计数据库表结构 ⏱️ 30分钟

**目标**: 设计 `quick_phrases` 表，存储用户的快捷短语数据

**表结构设计**:
```sql
CREATE TABLE IF NOT EXISTS quick_phrases (
  id TEXT PRIMARY KEY,
  title TEXT NOT NULL,           -- 短语标题（供列表显示）
  content TEXT NOT NULL,          -- 短语内容（插入到输入框）
  icon TEXT,                      -- 图标名称（可选）
  color TEXT,                     -- 颜色（可选）
  sort_order INTEGER DEFAULT 0,   -- 排序顺序
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL
);

CREATE INDEX IF NOT EXISTS idx_quick_phrases_sort
  ON quick_phrases(sort_order);
```

**涉及文件**:
- 新建 `storage/sqlite/migrations/0005_quick_phrases.ts`
- 修改 `storage/sqlite/db.ts`（注册新迁移）

**验收标准**:
- ✅ 迁移脚本执行成功
- ✅ 数据库表结构符合设计
- ✅ 索引创建成功

---

#### 任务 1.2：实现 QuickPhrasesRepository ⏱️ 1小时

**目标**: 创建数据访问层，封装快捷短语的 CRUD 操作

**接口定义**:
```typescript
interface QuickPhrase {
  id: string;
  title: string;
  content: string;
  icon?: string | null;
  color?: string | null;
  sortOrder: number;
  createdAt: number;
  updatedAt: number;
}
```

**Repository 方法**:
- `create(phrase: Omit<QuickPhrase, 'id' | 'createdAt' | 'updatedAt'>): Promise<QuickPhrase>`
- `getAll(): Promise<QuickPhrase[]>` - 按 sortOrder 排序
- `getById(id: string): Promise<QuickPhrase | null>`
- `update(id: string, updates: Partial<QuickPhrase>): Promise<void>`
- `delete(id: string): Promise<void>`
- `reorder(ids: string[]): Promise<void>` - 批量更新排序

**涉及文件**:
- 新建 `storage/repositories/quick-phrases.ts`
- 修改 `storage/core.ts`（添加 QuickPhrase 接口）

**验收标准**:
- ✅ 所有 CRUD 方法实现完整
- ✅ 错误处理完善
- ✅ 符合现有 Repository 模式
- ✅ TypeScript 类型安全

---

#### 任务 1.3：创建 useQuickPhrases Hook ⏱️ 45分钟

**目标**: 封装快捷短语的业务逻辑和状态管理

**Hook API**:
```typescript
interface UseQuickPhrasesReturn {
  phrases: QuickPhrase[];
  loading: boolean;
  error: Error | null;
  refresh: () => Promise<void>;
  create: (phrase: Omit<QuickPhrase, 'id' | 'createdAt' | 'updatedAt'>) => Promise<void>;
  update: (id: string, updates: Partial<QuickPhrase>) => Promise<void>;
  delete: (id: string) => Promise<void>;
  reorder: (ids: string[]) => Promise<void>;
}
```

**涉及文件**:
- 新建 `hooks/use-quick-phrases.ts`

**验收标准**:
- ✅ Hook 封装完整的业务逻辑
- ✅ 状态管理清晰（loading、error）
- ✅ 自动刷新机制
- ✅ 错误处理和用户提示

---

### 阶段 2：快捷短语管理界面

#### 任务 2.1：实现快捷短语列表页面 ⏱️ 2小时

**目标**: 替换占位页面，显示所有快捷短语，支持编辑、删除、排序

**UI 设计**:
- 使用 `List.Item` 显示每个短语（显示 title 和 content 预览）
- 支持长按拖动排序（可选，使用 react-native-reanimated）
- 右侧操作按钮：编辑、删除
- 底部悬浮"添加"按钮（FAB）
- 空状态提示

**涉及文件**:
- 修改 `app/settings/quick-phrases.tsx`
- 可选：新建 `components/settings/QuickPhrasesList.tsx`（列表组件）

**验收标准**:
- ✅ 列表正确显示所有短语
- ✅ 编辑按钮触发编辑对话框
- ✅ 删除按钮触发确认对话框
- ✅ 添加按钮触发创建对话框
- ✅ 空状态提示友好
- ✅ UI 符合应用整体风格

---

#### 任务 2.2：实现快捷短语编辑对话框 ⏱️ 1.5小时

**目标**: 创建/编辑快捷短语的弹窗界面

**UI 设计**:
- 使用现有弹窗组件作为基础容器
- 输入字段：
  - 标题（单行，必填）
  - 内容（多行，最多 500 字符，必填）
  - 图标选择（可选，预设图标列表）
  - 颜色选择（可选，预设颜色列表）
- 表单验证：标题和内容不能为空
- 按钮：取消、保存

**涉及文件**:
- 新建 `components/settings/QuickPhraseEditDialog.tsx`

**验收标准**:
- ✅ 表单验证正确
- ✅ 创建和编辑模式正常工作
- ✅ 保存后刷新列表
- ✅ 错误提示友好

---

#### 任务 2.3：实现快捷短语删除确认 ⏱️ 30分钟

**目标**: 删除前显示确认对话框

**交互设计**:
- 点击删除按钮 → 弹出 ConfirmDialog
- 确认后调用 `delete()` 方法
- 显示成功提示

**涉及文件**:
- 修改 `app/settings/quick-phrases.tsx`

**验收标准**:
- ✅ 确认对话框正确显示
- ✅ 删除操作执行成功
- ✅ 列表自动刷新
- ✅ 用户反馈清晰

---

### 阶段 3：快捷短语选择弹窗

#### 任务 3.1：创建快捷短语选择对话框组件 ⏱️ 2小时

**目标**: 在主界面显示快捷短语列表，支持选择和插入

**UI 设计**:
- 使用 `Portal` + `Modal` 实现全屏弹窗
- 顶部：标题栏 + 关闭按钮 + 跳转设置按钮
- 中间：短语列表（FlatList）
  - 每项显示图标、标题、内容预览
  - 点击即选中并关闭弹窗
- 空状态：提示用户去设置中添加短语
- 支持搜索/筛选（可选）

**组件 API**:
```typescript
interface QuickPhrasePickerDialogProps {
  visible: boolean;
  onDismiss: () => void;
  onSelect: (phrase: QuickPhrase) => void;
}
```

**涉及文件**:
- 新建 `components/chat/QuickPhrasePickerDialog.tsx`

**验收标准**:
- ✅ 弹窗动画流畅
- ✅ 列表渲染性能良好
- ✅ 选择操作响应迅速
- ✅ 跳转设置功能正常
- ✅ 空状态提示友好

---

#### 任务 3.2：集成选择对话框到 ChatInput 组件 ⏱️ 45分钟

**目标**: 在 ChatInput 中添加快捷短语选择功能

**功能实现**:
- 添加状态：`[phrasePickerVisible, setPhrasePickerVisible]`
- 添加回调：`handlePhraseSelect(phrase)` - 将 `phrase.content` 追加到 `message` 状态
- 渲染组件：
  ```tsx
  <QuickPhrasePickerDialog
    visible={phrasePickerVisible}
    onDismiss={() => setPhrasePickerVisible(false)}
    onSelect={handlePhraseSelect}
  />
  ```

**涉及文件**:
- 修改 `components/chat/ChatInput.tsx`

**验收标准**:
- ✅ 选择短语后正确插入到输入框
- ✅ 光标位置处理合理
- ✅ 弹窗关闭后输入框保持焦点

---

### 阶段 4：手势检测集成

#### 任务 4.1：在 MessageList 空白区域添加双击检测 ⏱️ 1.5小时

**目标**: 监听 MessageList 空白区域的双击手势，触发快捷短语选择弹窗

**实现方案**:
- 使用 `TapGestureHandler` 包裹 MessageList 的背景区域
- 配置 `numberOfTaps={2}` 检测双击
- 触发回调：`onDoubleTap={() => setPhrasePickerVisible(true)}`

**注意事项**:
- 确保手势不影响消息列表的滚动和点击
- 仅在空白区域有效（不能覆盖消息气泡）
- iOS 和 Android 平台测试

**涉及文件**:
- 修改 `components/chat/MessageList.tsx`
- 修改 `app/index.tsx`（传递回调）

**验收标准**:
- ✅ 双击空白区域触发弹窗
- ✅ 不影响消息列表滚动
- ✅ 不影响消息气泡点击
- ✅ 跨平台兼容性良好

---

#### 任务 4.2：添加手势视觉反馈 ⏱️ 30分钟

**目标**: 双击时提供触觉反馈和视觉提示（可选）

**实现方案**:
- 使用 `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light)` 提供触觉反馈
- 可选：显示短暂的波纹动画（使用 react-native-reanimated）

**涉及文件**:
- 修改 `components/chat/MessageList.tsx`

**验收标准**:
- ✅ 触觉反馈在支持的设备上正常工作
- ✅ 视觉反馈自然流畅
- ✅ 不影响性能

---

### 阶段 5：功能联调与测试

#### 任务 5.1：端到端功能测试 ⏱️ 1小时

**测试用例**:
1. ✅ 设置页面添加快捷短语
2. ✅ 编辑和删除快捷短语
3. ✅ 排序快捷短语（如已实现）
4. ✅ 主界面双击空白处打开弹窗
5. ✅ 选择短语后插入到输入框
6. ✅ 空状态提示正确显示
7. ✅ 跨平台测试（iOS、Android、Web）

**验收标准**:
- ✅ 所有测试用例通过
- ✅ 无明显 Bug
- ✅ 用户体验流畅

---

#### 任务 5.2：性能优化与边界情况处理 ⏱️ 1小时

**优化点**:
- 使用 `React.memo` 优化组件渲染
- FlatList 使用 `keyExtractor` 和 `getItemLayout`
- 处理数据库读写异常
- 处理用户快速点击的防抖

**涉及文件**: 相关组件

**验收标准**:
- ✅ 列表滚动流畅（60fps）
- ✅ 无内存泄漏
- ✅ 边界情况处理完善

---

#### 任务 5.3：文档更新 ⏱️ 30分钟

**文档内容**:
- 更新架构图，添加快捷短语模块
- 更新模块索引表
- 添加 Changelog 条目
- 更新 API 文档

**涉及文件**:
- `CLAUDE.md`
- `components/chat/CLAUDE.md`
- `storage/CLAUDE.md`
- `hooks/CLAUDE.md`

**验收标准**:
- ✅ 文档准确反映代码变更
- ✅ 示例代码可运行
- ✅ 架构图清晰易懂

---

## 待明确的设计决策

### 🔶 决策 1：快捷短语排序功能

**选项 A：支持拖动排序**
- ✅ 用户体验更好，自定义程度高
- ❌ 需要集成拖拽库，增加复杂度
- ⏱️ 预估额外工作量：2 小时

**选项 B：仅支持固定排序（按创建时间）**
- ✅ 实现简单，维护成本低
- ❌ 用户无法自定义顺序
- ⏱️ 预估额外工作量：0 小时

> 💡 **建议**: 先实现选项 B，后续根据用户反馈决定是否升级到选项 A

---

### 🔶 决策 2：快捷短语选择弹窗的展示位置

**选项 A：全屏对话框**
- ✅ 显示内容多，交互清晰
- ❌ 遮挡聊天内容

**选项 B：底部抽屉（Bottom Sheet）**
- ✅ 不完全遮挡，可上滑查看更多
- ❌ 需要额外的手势处理库（如 @gorhom/bottom-sheet）

**选项 C：悬浮气泡列表**
- ✅ 轻量，不打断对话
- ❌ 空间有限，难以展示长内容

> 💡 **建议**: 选项 A（全屏对话框），符合应用现有设计风格

---

### 🔶 决策 3：快捷短语的图标和颜色选择

**选项 A：预设图标+颜色库**
- ✅ 简单易用,风格统一
- ❌ 自定义程度低
- ⏱️ 预估工作量：1 小时（设计图标库）

**选项 B：纯文字标签，无图标颜色**
- ✅ 实现最简单
- ❌ 视觉单调，辨识度低
- ⏱️ 预估工作量：0 小时

**选项 C：支持 Emoji 作为图标**
- ✅ 表现力强，无需额外图标库
- ❌ 依赖系统 Emoji 支持
- ⏱️ 预估工作量：30 分钟

> 💡 **建议**: 选项 C（支持 Emoji），实现简单且表现力强

---

### 🔶 决策 4：手势触发方式

**选项 A：双击空白区域**
- ✅ 明确的手势，不易误触
- ❌ 可能不够直观，需要引导

**选项 B：长按空白区域**
- ✅ 手势较自然
- ❌ 可能与消息长按冲突

**选项 C：输入框增加快捷短语按钮**
- ✅ 最直观，发现性好
- ❌ 占用 UI 空间，不符合原需求

**选项 D：方案 A + 方案 C 同时支持**
- ✅ 兼顾直观性和简洁性
- ❌ 实现和维护成本较高

> 💡 **建议**: 选项 D（双击 + 按钮），提供多种触发方式

---

## 总体预估

### 📊 工作量统计

- **总开发时间**: 约 12-15 小时（不含可选功能）
- **核心任务**: 10 个主任务
- **涉及新文件**: 约 7 个
- **修改现有文件**: 约 5 个
- **数据库变更**: 1 个迁移脚本

### 🎯 关键里程碑

- ✅ **里程碑 1**: 数据层完成 → 可以存储和读取短语
- ✅ **里程碑 2**: 设置页面完成 → 用户可以管理短语
- ✅ **里程碑 3**: 选择弹窗完成 → 用户可以使用短语
- ✅ **里程碑 4**: 手势集成完成 → 完整功能可用

### ⚠️ 风险识别

- 🔶 手势检测可能与现有滚动手势冲突（需细致调试）
- 🔶 跨平台兼容性（Web 端手势检测可能不同）
- 🔶 性能问题（大量短语时列表渲染性能）

### 📋 建议优先级

1. **高优先级**: 任务 1.1 ~ 2.1（数据层 + 基础管理）
2. **中优先级**: 任务 3.1 ~ 4.1（弹窗 + 手势）
3. **低优先级**: 任务 4.2、5.2（优化和增强）

---

## 下一步行动

1. 📝 **等待用户确认设计决策**（决策 1-4）
2. 🚀 **用户确认后开始执行实施**
3. 📊 **跟踪任务进度并及时同步**

---

_规划文档版本: v1.0_
_最后更新: 2025-11-13_
