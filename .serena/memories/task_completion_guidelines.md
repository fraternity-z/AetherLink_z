# 任务完成后的检查清单

- 运行 `npm run lint` 确认无明显风格/语法问题
- 关键交互路径自测：
  - 打开侧边栏 > 设置页 > 上下文数目滑块是否支持 0–20
  - 将上下文设为 0，发送消息时应只发送当前用户消息（不包含系统提示和历史）
  - 将上下文设为 >0，发送消息应包含系统提示与最近 N 轮历史
- 不修改无关代码，确保变更范围最小化
- 遇到不确定需求，优先询问澄清